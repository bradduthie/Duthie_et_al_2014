double var(double *vec, int n){
	
	int i;
	
	double s, var;
	
	s   = 0;
	var = 0;
	
	for(i=0; i<n; i++){
		s = (1/n)*(s + vec[i]);
	}
	
	for(i=0; i<n; i++){
		var = (1/n)*(var + (vec[i] - s)*(vec[i] - s));
	}

	return var;